import React from 'react';

import { View } from 'react-native';

// import { Container } from './styles';

const RecipeDetailScreen = ({ navigation }) => {
  console.log('item', navigation.state.params);

  return (<View />);
};

export default RecipeDetailScreen;
